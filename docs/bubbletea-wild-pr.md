# PR to charmbracelet/bubbletea: "Bubble Tea in the wild"

The Bubble Tea README has a section listing community projects. Open a PR
adding readpanda.

## Find the section

In `README.md`, look for the heading:

```
## Bubble Tea in the wild
```

Followed by an alphabetised list of links. Insert in alphabetical order.

## Line to add

```markdown
* [readpanda](https://github.com/sonirico/readpanda): k9s-style terminal UI for Redpanda and Kafka, with inline Avro/Protobuf decoding via Schema Registry
```

## PR title

```
docs: add readpanda to "Bubble Tea in the wild"
```

## PR body

```
readpanda is a Bubble Tea TUI for Redpanda / Kafka clusters: collapsible
topics tree, tabbed topic detail (messages / consumers / partitions /
configuration / ACL), live tail with inline JSON / Avro / Protobuf decoding
via Confluent Schema Registry, rpk profile contexts.

Repo: https://github.com/sonirico/readpanda
v0.1.0 release: https://github.com/sonirico/readpanda/releases/tag/v0.1.0
```

## Reddit / Slack mirrors

Once the Bubble Tea PR is in (merged or pending), copy the same line into:

- `r/golang` with a short post. They like Bubble Tea showcases.
- Redpanda community Slack `#community` or `#tools`.
- Charm's Discord `#showcase` channel.
